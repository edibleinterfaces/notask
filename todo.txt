update:
    store build version as part of app key, e.g. "ei-notask-0.0.1" in client storage and check it in app init code.
    if different, apply upgrade.
ui:
    build modal window for color theme settings
    fix dashboard icon alignments at all screen sizes
    add textured background to listsview
    convert setInterval to request animation frame
    schedule a reminder should be a button
    complete toggle, sort functionality by completed

storage:
    google drive sync code: should work for all apps
    look into time to load, a little slow

build:
    x convert font awesome to cdn link 


